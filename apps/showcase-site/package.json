{
  "name": "quant-showcase-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Static portfolio landing page for the quant workspace (Netlify-hosted).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
